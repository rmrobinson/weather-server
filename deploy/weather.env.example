# Required — InfluxDB API token with read/write bucket and task permissions.
# See README.md for how to create this token.
INFLUX_TOKEN=

# Optional — Pre-shared key required on all API and gRPC requests.
# Leave empty (or omit entirely) to disable authentication.
WEATHER_PSK=
